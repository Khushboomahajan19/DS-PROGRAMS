// This is also called two way linklist.In this list each node contains three fields-[prev,next,val].Eachnode contains the adress of 
// their neighbour(both sides).The first node prev address as NUL whereas the last node next address contains NULL
// We can move forward and backward easily by address of next and previous 

// HEADRER LINKLIST-This list contain an extra node called header node,this node will carry some information related to linklist.
// For ex-No. of nodes,address of middle node,Address of last node etc.